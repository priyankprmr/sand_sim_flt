// Fixed resolution for the game's screen
final screenWidth = 400.0;
final screenHeight = 400.0;

// Size of each cell in the grid (in pixels)
const int pixelSize = 4;

final col = (screenWidth / pixelSize).toInt();
final row = (screenHeight / pixelSize).toInt();